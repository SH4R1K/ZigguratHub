# Опционально: переопределить адрес API для сборки фронта.
# По умолчанию (пусто) запросы идут на /api/* того же origin:
#   - dev: Vite-прокси (vite.config.ts) -> http://localhost:5080
#   - Docker: nginx-прокси (client/nginx.conf) -> контейнер server
# VITE_API_BASE=http://localhost:5080